/* Local Imports */
import { REACT_APP_BASE_URL } from '@env'

// --------------------------------------------------------------------------------------------------

export const envConfig = {
	base: {
		baseUrl: REACT_APP_BASE_URL,
	},
}
