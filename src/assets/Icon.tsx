import React from 'react'
import { ViewStyle } from 'react-native'

type IconProps = {
	width?: number
	height?: number
	color?: string
	isFilled?: boolean
	styles?: ViewStyle
}

const Icon = {}

export default Icon
